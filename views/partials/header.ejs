<!DOCTYPE html>
<html>
	<head>
		<title>	DOGS ONLY </title>
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
		<link href="https://fonts.googleapis.com/css?family=Coustard&display=swap" rel="stylesheet">

		<script src="https://kit.fontawesome.com/20a5c1cf29.js"></script>
		
		<style>
			
			body {
				
				font-family: 'Coustard', serif;
			}
			html, body {
			  height: 100%;
			  
			}

			.as-full {
			  width: 100%;
			  height: 100%;
			}
			.form-control {
			  padding-left: 30px;
			  padding-right: 30px;
			}
			/*
			.form-control + .fa {
			  position: absolute;
			  left: 0;
			  padding: 0px 30px;
			}*/

			.iconinput {
				position: relative;
				z-index: 1;
				left: 10px;
				top: -31px;
				width: 0;
			}
			.form-check-label {
				padding-bottom: 10px;
			}
			.form-group {
				margin-bottom : 0px;
			}
		</style>
	</head>
	
		<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
			<div class="container">
				<div class="navbar-header">
					<a class="navbar-brand" href="/"><i class="fas fa-paw"></i>  DOGSONLY</a>
				</div>

				<div class="navbar-collapse ">
					<ul class="navbar-nav mr-auto">
						<li class="nav-item active lineNav" data-toggle="tooltip"><a class="nav-link" href="/dogs"> Dogs Collection </a></li>
					</ul>
					<ul class="navbar-nav ml-auto">
						<% if (!currentUser) { %>
							<li class="nav-item active "><a class="nav-link" href="/login"> Login </a></li>
							<li class="nav-item"><a class="nav-link" href="/signup"> Sign Up </a></li>
						<% } else { %>
							<li><a class="nav-item" href="#">Signed In as <%= currentUser.username %> </a></li>
							<li class="nav-item"><a class="nav-link" href="/logout"> Log Out </a></li>
						<% } %>
						
						
					</ul>
				</div>
			</div>
		</nav>
		
	
	<div class="container pt-3">
			<% if(error && error.length > 0){ %>
				<div class="alert alert-danger" role="alert">
					<%= error %>
				</div>
			<% } %>
			
			<% if(success && success.length > 0){ %>
				<div class="alert alert-success" role="alert">
					<%= success %>
				</div>
			<% } %>
			
	</div>
	